name: vr_player
description: A VR/360 Player For Flutter

publish_to: "none"

version: 1.0.0+10

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # webview_flutter: ^1.0.5
  webview_flutter:
    git:
      url: https://github.com/raj457036/plugins.git
      path: packages/webview_flutter

  device_info: ^0.4.2+8

flutter:
  uses-material-design: true
